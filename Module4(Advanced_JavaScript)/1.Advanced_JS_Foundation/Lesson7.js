const dreamHoliday = {
    destination : "Maldives",
    activity : "Scuba Diving",
    accomodation :"Overwater Bungalow",
    companion :"Best Friend"
}

const { destination, activity, accomodation, companion } = dreamHoliday;

console.log(`My dream holiday is to go to ${destination} and enjoy ${activity}. I would stay in an ${accomodation} with my ${companion}.`);

/* define print function */
function print(msg) {
    console.log(msg)
}